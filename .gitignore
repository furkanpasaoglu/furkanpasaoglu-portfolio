# ── Client (Node / Vite) ──
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# ── Server (.NET) ──
[Bb]in/
[Oo]bj/
*.user
*.suo
*.userprefs
.vs/

# ── Runtime / secrets ──
.env
.env.local
.env.*.local
uploads/

# ── Database volume (docker local) ──
pgdata/

# ── Editor ──
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.ntvs*
*.njsproj
*.sln.lock
*.sw?

# ── Docker ──
# Local dev overrides must be copied from docker-compose.override.yml.example.
# Do NOT commit the real override file — it exposes Postgres/API ports to the host
# and would open those same ports in prod (Dokploy, etc.).
docker-compose.override.yml

# ── Tool caches ──
.vite/
.claude/
